> 🌐 本文档由 [ultraworkers/claw-code](https://github.com/ultraworkers/claw-code) 翻译,英文原版见原项目。

# 安全策略

## 支持的版本

安全修复面向当前 `main` 分支以及(如果有)最新发布的 release 构件。较旧的实验性分支不受支持,除非维护者明确标记其为受支持。

## 报告漏洞

对于疑似漏洞,**不要**开公开 issue。请在可用时使用 `ultraworkers/claw-code` 的 GitHub 私有漏洞报告功能,或通过仓库公布的支持渠道联系维护者,并附上最小化的、非破坏性的复现步骤。

请包含:

- 受影响的命令、crate 或工作流;
- 操作系统与 shell,尤其是 Windows/PowerShell 路径问题;
- 是否涉及真实凭据、MCP 服务器、插件或工作区文件系统访问;
- 预期影响以及任何安全的验证步骤(PoC)。

不要包含真实的 API key、私有 prompt、含敏感信息的会话记录,或会修改第三方系统的利用载荷。

## 范围

在范围内:

- 工作区路径穿越或符号链接逃逸;
- 权限绕过、沙箱状态误报,或不安全的工具执行;
- 日志、JSON 输出、遥测、文档或示例中的凭据泄露;
- 可能意外执行代码或泄露密钥的插件、hook、MCP、provider 或配置行为。

不在范围内:

- 社会工程学;
- 没有实际安全影响的拒绝服务;
- 依赖"本地开发者凭据已被攻陷"这一前提的问题;
- 针对第三方 provider 或上游工具、且与 Claw Code 集成无关的报告。

## 处理预期

维护者会尽快确认有效的私有报告,在修复或缓解措施可用之前保持讨论私密,并在报告者要求且合适的情况下予以署名致谢。
